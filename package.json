{
  "name": "8000000",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "analyze": "cross-env ANALYZE=true next build",
    "export": "next export",
    "bundle-analyzer": "npx @next/bundle-analyzer .next",
    "lighthouse": "node scripts/performance-audit.js",
    "audit": "npm run build && npm run lighthouse",
    "test:performance": "npm run build && node scripts/performance-audit.js http://localhost:3000",
    "vercel:build": "npm run build && npm run lighthouse -- http://localhost:3000 ./dist/reports",
    "check:contrast": "node -e \"require('./utils/contrast-checker').enableContrastDebugging()\"",
    "optimize:images": "next-optimized-images",
    "precommit": "npm run lint && npm run audit"
  },
  "dependencies": {
    "critters": "^0.0.23",
    "framer-motion": "^12.15.0",
    "next": "^14.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "sharp": "^0.32.0",
    "web-vitals": "^5.0.2"
  },
  "devDependencies": {
    "@next/bundle-analyzer": "^14.0.0",
    "@types/node": "^20.0.0",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "cross-env": "^7.0.3",
    "eslint": "^8.0.0",
    "eslint-config-next": "^14.0.0",
    "lighthouse": "^11.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.17.0"
  },
  "browserslist": {
    "production": [
      ">0.3%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
